# Watchdog ping: if unresponsive, reboot:
ping-count = 5       # ping 5 times
ping = 192.168.10.210   # ping test destination IP address

interval = 30        # perform watchdog checks every 30 seconds